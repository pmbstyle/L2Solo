# Choose one AI backend below and uncomment its section.
# Optional staged progression cap. Omit this section to use General.maxLevel.
; [Progression]
; contentCap = 40
#
#
# [OpenRouter]
# enabled = true
# apiKey = sk-or-v1-your-key-here
# apiUrl = https://openrouter.ai/api/v1/chat/completions
# model = openai/gpt-5.6-luna
# Optional cheap model for ambiguous party-chat routing; empty keeps deterministic routing only.
# partyRouterModel =
# Model API capabilities belong here, not in the runtime code.
# Leave temperature empty when the selected model does not accept it.
# temperature =
# completionLimitParam = max_tokens
# strictSchema = true
# providerOrder = OpenAI
# providerSort = price
# allowFallbacks = false
# requireParameters = true
# debug = true

# Instead of OpenRouter, use any OpenAI-compatible Chat Completions endpoint.
# Remove/rename the [OpenRouter] section above when using this section.
#
# [AI]
# enabled = true
# apiUrl = http://127.0.0.1:1234/v1/chat/completions
# apiKey = lm-studio
# model = exact-model-id
# partyRouterModel = exact-model-id
# temperature = 0.35
# completionLimitParam = max_tokens
# strictSchema = false
# reasoningEffort = off

; Optional override of autonomous bot PvP frequency; restart the server to apply.
; [BotPopulation]
; pvpAggression = 0.5
; Range 0..1: 0 disables initiation, 0.5 preserves balance, 1 maximizes eligible escalation.
; Defense and party assistance remain active; zero chances and combat guards remain.
; Lower values favor retreat; higher values accept more risk before retreating.

[Langfuse]
enabled = false
envFile =
baseUrl = http://localhost:3000
capturePayloads = true
debug = true
