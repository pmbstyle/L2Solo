// Wall-clock duration from death, independent of the accelerated world clock.
// Raid bosses have a fixed window; ordinary NPCs retain datapack respawn/bias.
const RESPAWN_DELAY_MS = 5 * 60 * 60 * 1000;

module.exports = { RESPAWN_DELAY_MS };
